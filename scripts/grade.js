class Grade
{
    constructor(section_name, percent_weight, current_grade)
    {
        this.section_name = section_name
        this.percent_weight = percent_weight
        this.current_grade = current_grade
    }
}